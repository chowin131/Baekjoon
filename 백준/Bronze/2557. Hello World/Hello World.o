extern printf
section .data
    msg db "Hello World!", 0xa, 0
section .text
    global main
main:
    sub rsp, 8
    lea rdi, [rel msg]
    xor rax, rax
    call printf
    add rsp, 8
    xor rax, rax
    ret